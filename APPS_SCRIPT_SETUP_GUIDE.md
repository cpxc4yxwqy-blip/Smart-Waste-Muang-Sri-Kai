# คู่มือติดตั้ง Google Apps Script Web App

## 📋 สิ่งที่ต้องเตรียม

1. ✅ Google Sheets ที่สร้างไว้แล้ว
2. ✅ ไฟล์ `apps-script-code.gs` (มีในโปรเจกต์แล้ว)

---

## 🚀 ขั้นตอนการติดตั้ง

### ขั้นที่ 1: เปิด Apps Script Editor

1. เปิด Google Sheets ของคุณ
2. คลิกเมนู **Extensions** → **Apps Script**
3. หน้าต่าง Apps Script Editor จะเปิดขึ้น

### ขั้นที่ 2: วางโค้ด

1. ลบโค้ดเก่าทั้งหมดใน Editor (ถ้ามี)
2. เปิดไฟล์ `apps-script-code.gs` ในโปรเจกต์นี้
3. **คัดลอกโค้ดทั้งหมด** (Ctrl+A → Ctrl+C)
4. **วางลงใน Apps Script Editor** (Ctrl+V)
5. บันทึก (Ctrl+S หรือคลิกไอคอน 💾)
6. ตั้งชื่อโปรเจกต์ เช่น "Smart Waste Web App"

### ขั้นที่ 3: สร้าง Header Row (ตัวเลือก)

**วิธีที่ 1: เรียกใช้ฟังก์ชัน `createHeaderRow()`**

1. ใน Apps Script Editor เลือกฟังก์ชัน `createHeaderRow` จาก dropdown
2. คลิกปุ่ม **Run** (▶️)
3. ครั้งแรกจะขออนุญาต → คลิก **Review Permissions** → เลือกบัญชี Google → **Allow**
4. รอสักครู่ → เสร็จแล้วจะเห็น Header Row ใน Google Sheets

**วิธีที่ 2: เรียกผ่าน Web App (หลัง Deploy)**

เปิดเบราว์เซอร์ไปที่:
```
https://script.google.com/macros/s/YOUR_WEB_APP_ID/exec?action=createHeader
```

**หมายเหตุ:** Header Row จะถูกสร้างอัตโนมัติเมื่อมีการบันทึกข้อมูลครั้งแรกด้วย

### ขั้นที่ 4: Deploy as Web App

1. คลิกปุ่ม **Deploy** (ด้านบนขวา) → **New deployment**
2. คลิกไอคอน ⚙️ (Gear icon) → เลือก **Web app**
3. ตั้งค่าดังนี้:
   - **Description:** Smart Waste Dashboard API v1.0
   - **Execute as:** Me (อีเมลของคุณ)
   - **Who has access:** **Anyone** (สำคัญ!)
4. คลิก **Deploy**
5. **คัดลอก Web App URL** (จะมีรูปแบบ: `https://script.google.com/macros/s/AKfycby.../exec`)

### ขั้นที่ 5: ตั้งค่า Web App URL ในแอป

1. เปิดแอป Smart Waste Dashboard
2. ไปที่ **Settings** (⚙️ ใน Sidebar)
3. เลือกแท็บ **Google Sheets**
4. วาง **Web App URL** ในช่อง "Google Apps Script Web App URL"
5. คลิก **"บันทึกการตั้งค่า"**

---

## ✅ ทดสอบการทำงาน

### วิธีที่ 1: ทดสอบผ่าน Apps Script Editor

1. ใน Apps Script Editor เลือกฟังก์ชัน `testAppendData`
2. คลิก **Run** (▶️)
3. ดู Execution log (View → Logs) → ควรเห็น `{ success: true, ... }`
4. เปิด Google Sheets → ควรเห็นข้อมูลทดสอบถูกเพิ่มเข้ามา

### วิธีที่ 2: ทดสอบผ่าน PowerShell

```powershell
$webAppUrl = "https://script.google.com/macros/s/YOUR_WEB_APP_ID/exec"
$spreadsheetId = "YOUR_SPREADSHEET_ID"

$body = @{
  action = 'append'
  spreadsheetId = $spreadsheetId
  sheetName = 'WasteData'
  data = @(
    'test-' + (Get-Date).Ticks,    # ID
    2025,                            # Year
    12,                              # Month
    22.5,                            # General Waste
    13.2,                            # Organic Waste
    7.8,                             # Recyclable Waste
    1.5,                             # Hazardous Waste
    45.0,                            # Total
    5000,                            # Population
    'ทดสอบ',                         # Category
    '',                              # Sub Category
    'ทดสอบจาก PowerShell',          # Notes
    (Get-Date).ToString('o'),        # Created At
    (Get-Date).ToString('o'),        # Updated At
    'PowerShell Tester'              # Created By
  )
} | ConvertTo-Json -Depth 5

$response = Invoke-RestMethod -Uri $webAppUrl -Method Post -Body $body -ContentType 'application/json'
$response
```

**ผลลัพธ์ที่คาดหวัง:**
```json
{
  "success": true,
  "message": "Data saved successfully at row 2",
  "data": {
    "rowNumber": 2,
    "sequenceNumber": 1
  }
}
```

### วิธีที่ 3: ทดสอบจากแอป

1. เปิดแอป Smart Waste Dashboard
2. ไปที่ **Data Entry**
3. กรอกข้อมูล → คลิก **"บันทึกข้อมูล"**
4. ดู Google Sheets → ข้อมูลควรปรากฏทันที

---

## 📊 โครงสร้าง Header Row ใน Google Sheets

| คอลัมน์ | ชื่อคอลัมน์ | คำอธิบาย | ตัวอย่าง |
|---------|-------------|----------|----------|
| A | ลำดับ | Auto-increment (1, 2, 3, ...) | 1 |
| B | ID | รหัสอ้างอิงเฉพาะ | 1733308800000 |
| C | ปี (ค.ศ.) | ปีคริสต์ศักราช | 2025 |
| D | เดือน | เดือน 1-12 | 12 |
| E | ขยะทั่วไป (ตัน) | General Waste | 22.5 |
| F | ขยะอินทรีย์ (ตัน) | Organic Waste | 13.2 |
| G | ขยะรีไซเคิล (ตัน) | Recyclable Waste | 7.8 |
| H | ขยะอันตราย (ตัน) | Hazardous Waste | 1.5 |
| I | ปริมาณขยะรวม (ตัน) | Total Waste | 45.0 |
| J | จำนวนประชากร | Population | 5000 |
| K | หมวดหมู่ | Category | - |
| L | หมวดหมู่ย่อย | Sub Category | - |
| M | หมายเหตุ | Notes | ข้อมูลทดสอบ |
| N | วันที่บันทึก | Created At (ISO 8601) | 2025-12-04T10:30:00Z |
| O | วันที่แก้ไข | Updated At (ISO 8601) | 2025-12-04T10:30:00Z |
| P | ผู้บันทึกข้อมูล | Created By / Updated By | Admin |

---

## 🎨 คุณสมบัติของ Apps Script

### ✅ Auto-numbering (ลำดับ)
- คำนวณลำดับอัตโนมัติจากจำนวนแถว
- ไม่ต้องส่งค่าจากแอป

### ✅ Auto-create Header
- สร้าง Header Row อัตโนมัติเมื่อ Sheet ว่าง
- จัดรูปแบบสวยงาม (สีเขียว พื้นหลังขาว)

### ✅ Formatting
- จัดชิดกลางสำหรับตัวเลข
- Zebra striping (สลับสีแถว)
- Freeze header row

### ✅ Error Handling
- ตรวจสอบข้อมูลที่จำเป็น
- ส่ง JSON response พร้อม error message

---

## 🔧 การแก้ไขปัญหา

### ❌ "Missing spreadsheetId"
**สาเหตุ:** แอปไม่ได้ส่ง Spreadsheet ID  
**แก้ไข:** ตรวจสอบว่าตั้งค่า Spreadsheet ID ใน Settings → Google Sheets แล้ว

### ❌ "Authorization required"
**สาเหตุ:** ยังไม่ได้ให้สิทธิ์ Apps Script  
**แก้ไข:** Run ฟังก์ชัน `createHeaderRow` ครั้งแรก → Allow permissions

### ❌ "Sheet not found"
**สาเหตุ:** ไม่มี Sheet ชื่อ WasteData  
**แก้ไข:** Script จะสร้างอัตโนมัติ หรือสร้างด้วยตนเอง

### ❌ ข้อมูลไม่ปรากฏใน Google Sheets
**ตรวจสอบ:**
1. Web App URL ตั้งค่าถูกต้องหรือไม่ (ลงท้ายด้วย `/exec`)
2. Deploy เป็น "Anyone" หรือยัง
3. ดู Execution log ใน Apps Script (View → Executions)

---

## 📝 หมายเหตุสำคัญ

1. **Deploy ใหม่ทุกครั้งที่แก้โค้ด:**
   - Deploy → Manage deployments → ⚙️ Edit → Version: New version → Deploy
   - URL จะเหมือนเดิม ไม่ต้องเปลี่ยนใน Settings

2. **Execution Logs:**
   - ดูได้ที่ Apps Script Editor → View → Executions
   - ช่วยแก้ปัญหาเมื่อมี error

3. **Sheet Name:**
   - Default: `WasteData`
   - เปลี่ยนได้ใน Settings → Google Sheets → Sheet Name

4. **Auto-increment:**
   - คอลัมน์ "ลำดับ" จะเพิ่มอัตโนมัติ
   - ไม่ต้องส่งค่านี้จากแอป

---

## ✅ Checklist

- [ ] คัดลอกโค้ดจาก `apps-script-code.gs` ไปวางใน Apps Script Editor
- [ ] บันทึกโปรเจกต์ (Ctrl+S)
- [ ] Run ฟังก์ชัน `createHeaderRow` → Allow permissions
- [ ] Deploy as Web App (Execute as: Me, Who has access: Anyone)
- [ ] คัดลอก Web App URL
- [ ] วาง URL ใน Settings → Google Sheets → Google Apps Script Web App URL
- [ ] คลิก "บันทึกการตั้งค่า"
- [ ] ทดสอบด้วย `testAppendData` หรือจากแอป
- [ ] ตรวจสอบ Google Sheets ว่ามีข้อมูลปรากฏ

---

## 🎯 สำเร็จแล้ว!

เมื่อทำตาม Checklist ครบ ระบบ Google Sheets Sync จะทำงานได้ 100% ✅

ข้อมูลจากแอปจะถูกบันทึกไปยัง Google Sheets อัตโนมัติทันทีที่กดปุ่ม "บันทึกข้อมูล"
